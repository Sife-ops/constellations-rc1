query UserBookmarkCategory($userId: Int!) {
  user(id: $userId) {
    id
    username
    bookmarks {
      id
      url
      description
      categories {
        id
        name
      }
    }
  }
}
